# Suggestions de code (actions rapides)

- Ajouter un mode `--report` HTML avec resume des stats.
- Ajouter un `--strict` pour bloquer si PII detectee.
- Ajouter un format de sortie JSONL pour gros volumes.
